services:
  app:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./corpus:/app/corpus
      - chroma-data:/app/chroma_data
    env_file:
      - .env
    restart: unless-stopped

volumes:
  chroma-data:
